import React from 'react';
import DefaultNavbar from '@/containers/layout/components/Navbars/DefaultNavbar';

const Header = () => {
    return (
        <section className="page-header">
            <DefaultNavbar/>
        </section>
    );
};

export default Header;
